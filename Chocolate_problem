/* Given an array A of positive integers of size N, where each value represents number of chocolates in a packet. Each packet can have variable number of chocolates. There are M students, the task is to distribute chocolate packets such that :
1. Each student gets one packet.2. The difference between the number of chocolates given to the students having packet with maximum chocolates and student having packet with minimum chocolates is minimum.*/
/*
Example:
Input:
2
8
3 4 1 9 56 7 9 12
5
7
7 3 2 4 9 12 56
3

Output:
6
2

Explanation:
Testcase 1: The minimum difference between maximum chocolates and minimum chocolates is 9-3=6 */
/* Created by Soumya Banerjee on 20th September,2019 */
/* Code done in C++ */
#include <bits/stdc++.h>
using namespace std;
#define ll long long int
ll a[10000000];
ll candy(ll n,ll m)
{
    ll i,j,k,min=LLONG_MAX;
    sort(a,a+n);
    for(i=0;i<n-m+1;i++)
    {
        if((a[i+m-1]-a[i])<min)
        min=(a[i+m-1]-a[i]);
    }
    return min;
}
int main() {
	//code
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	ll t,n,m,i,j,k;
	cin>>t;
	while(t--)
	{
	    cin>>n;
	    for(i=0;i<n;i++)
	    cin>>a[i];
	    cin>>m;
	    cout<<candy(n,m)<<"\n";
	}
	return 0;
}
